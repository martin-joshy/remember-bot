version: "2"
sql:
  - engine: "sqlite"
    schema: "sql/schema.sql"
    queries: "sql/queries.sql"
    gen:
      go:
        package: "db"
        out: "db"
        emit_interface: true
        emit_json_tags: true
        emit_pointers_for_null_types: true
